package regularEx;

public class Test {
    public static void main(String[] args) {
        String originalText, modifiedText;
        originalText = "This is test."; //assign string to 'originalText'
        modifiedText = originalText.replaceAll("[aeiou]", "@");

    }
}
